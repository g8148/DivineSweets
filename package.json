{
  "name": "divine-sweets",
  "private": true,
  "workspaces": [
    "app",
    "api",
    "shared"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --env-file-if-exists=api/.env --import ./hooks/registrar.mjs --test",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
